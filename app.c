
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    if(argc < 2) {
        printf("Error: Invalid amount of arguments.\n");
        exit(EXIT_FAILURE);
    }

    return 0;
}

